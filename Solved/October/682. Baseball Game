class Solution:
    def calPoints(self, operations: List[str]) -> int:
        record = []
        for i in range(len(operations)):
            if operations[i] == "+":
                if len(record) >= 2:
                    record.append(record[-1] + record[-2])
            elif operations[i] == "D":
                if len(record) >= 1:
                    record.append(record[-1] * 2);
            elif operations[i] == "C":
                record = record[:-1]
            else:
                record.append(int(operations[i]))
        
        return sum(record)


Good to remember syntax and slicing in Python.